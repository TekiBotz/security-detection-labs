# Site settings
title: Security Detection Labs
description: Home-lab write-ups on detecting network attacks with Suricata IDS and Wazuh — an Nmap scanning-probe detection lab and a tmNIDS NIDS-testing lab.
url: "https://NSPAD.github.io"

# Markdown options
markdown: kramdown

# Theme settings (Jekyll theme)
#theme: jekyll-theme-hacker

remote_theme: pages-themes/hacker@v0.2.0
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one
